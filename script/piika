#/usr/bin/env gosh

(import (scheme base)
        (scheme process-context)
        (piika cli))

(define (main args)
  (run  args))

(main (command-line))

;; Local Variables:
;; mode: scheme
;; End:
